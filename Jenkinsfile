pipeline {
    agent any 
    stages {
        stage('clone repo and clean it') { 
            steps {
            sh "git clone https://github.com/hindhilali/layouts2.git"
            
            }
        }
        stage('Test') { 
            steps {
                 echo "successfull again"
            }
        }
        stage('Deploy') { 
            steps {
                  echo "successfull again" 
            }
        }
    }
}
